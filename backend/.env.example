# Copia este archivo como ".env" y completa los valores reales.
# El archivo .env NO se sube a git (está en .gitignore).

# Cadena de conexión a la base de datos PostgreSQL de Supabase.
# La obtienes en: Supabase > Project Settings > Database > Connection string (URI)
DATABASE_URL="postgresql://usuario:contrasena@host:5432/postgres"

# Clave secreta para firmar los tokens de sesión (login). Poné un texto largo y aleatorio.
JWT_SECRET="cambia-esto-por-un-texto-largo-y-secreto"

# Puerto donde corre el backend
PORT=4000
